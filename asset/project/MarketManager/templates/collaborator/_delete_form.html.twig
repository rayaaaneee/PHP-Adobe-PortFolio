<form method="post" action="{{ path('app_collaborator_delete', {'id': collaborator.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaborator.id) }}">
    <button class="btn">Delete</button>
</form>
